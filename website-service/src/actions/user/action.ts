// =======================================================================================

export const getConnectedUser = async (): Promise<ApiResponse<null>> => {
  return {
    success: true,
    message: "Connexion réussie",
    data: null,
  };
};

// =======================================================================================

export const getConnectedEmployee = async (): Promise<ApiResponse<null>> => {
  return {
    success: true,
    message: "Connexion réussie",
    data: null,
  };
};

// =======================================================================================
