import { userIsConnected } from "@/lib/session";
import { employeeIsConnected } from "@/lib/sessionEmployee";
import { nextCache } from "@/lib/utilsServer";

// =======================================================================================

export const getConnectedUser = async () => {};

// =======================================================================================

export const getConnectedEmployee = async () => {};

// =======================================================================================
